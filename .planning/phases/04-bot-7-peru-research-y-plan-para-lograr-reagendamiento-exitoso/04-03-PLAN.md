---
phase: 04-bot-7-peru-research-y-plan-para-lograr-reagendamiento-exitoso
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - CONFIG-01
  - CONFIG-02
  - VERIFY-01

must_haves:
  truths:
    - "Bot 7 is using direct provider (not webshare) for lower latency"
    - "Bot 7 targetDateBefore is widened to 2026-07-01 to catch more date sightings"
    - "Code changes from Plan 01 and 02 are deployed to RPi where Bot 7 runs"
    - "Bot 7 poll chain is active and healthy after config change"
    - "Bot 7 speculativeTimeFallback is still false after deploy (user enables via Plan 02 checkpoint)"
  artifacts: []
  key_links:
    - from: "PUT /api/bots/7"
      to: "poll-visa.ts next iteration"
      via: "Bot reads config fresh each poll from DB"
      pattern: "proxyProvider.*direct"
---

<objective>
Switch Bot 7 to optimized configuration (direct provider + wider target window), deploy code changes, and verify the system is armed and healthy. The speculative fallback deploys disabled (speculativeTimeFallback=false) -- user enables it separately after reviewing dry-run logs in Plan 02's checkpoint.

Purpose: Research showed both historical successes used `direct` provider, and widening `targetDateBefore` increases date sighting frequency (12 sightings in 54 days with current window). This plan applies the config changes and confirms everything is working.

Output: Bot 7 running with direct provider, wider target window, diagnostic logging active, speculative fallback code deployed but disabled, ready to catch next bookable date.
</objective>

<execution_context>
@/Users/juanortega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanortega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bot-7-peru-research-y-plan-para-lograr-reagendamiento-exitoso/04-RESEARCH.md
@.planning/phases/04-bot-7-peru-research-y-plan-para-lograr-reagendamiento-exitoso/04-01-SUMMARY.md
@.planning/phases/04-bot-7-peru-research-y-plan-para-lograr-reagendamiento-exitoso/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy code changes to RPi and switch Bot 7 config</name>
  <files><!-- deploy+config only, no source files modified --></files>
  <read_first>
    - CLAUDE.md (deployment commands: npm run deploy:rpi)
    - .planning/phases/04-bot-7-peru-research-y-plan-para-lograr-reagendamiento-exitoso/04-RESEARCH.md (lines 295-296: config changes via API)
  </read_first>
  <action>
**Step 1: Push DB schema change**

Run `npm run db:push` to apply the `speculative_time_fallback` column added in Plan 02 to the production database. This adds the column with default false -- all existing bots remain unaffected.

**Step 2: Deploy code to RPi**

Run `npm run deploy:rpi` to sync all code changes from Plan 01 and 02 to the RPi where Bot 7 runs (pollEnvironments: ["dev"]).

**Step 3: Switch Bot 7 to direct provider**

```bash
curl -s -X PUT "https://visa.homiapp.xyz/api/bots/7" \
  -H "Content-Type: application/json" \
  -d '{"proxyProvider": "direct"}' | jq '{status, proxyProvider}'
```

Expected response: `{ "status": "active", "proxyProvider": "direct" }`

**Step 4: Widen targetDateBefore**

```bash
curl -s -X PUT "https://visa.homiapp.xyz/api/bots/7" \
  -H "Content-Type: application/json" \
  -d '{"targetDateBefore": "2026-07-01"}' | jq '{status, targetDateBefore}'
```

Expected response: `{ "status": "active", "targetDateBefore": "2026-07-01" }`

**Step 5: Verify speculativeTimeFallback is still false**

```bash
curl -s "https://visa.homiapp.xyz/api/bots/7" | jq '{speculativeTimeFallback}'
```

Expected: `{ "speculativeTimeFallback": false }` -- the flag is NOT enabled here. User enables it via Plan 02's checkpoint after reviewing dry-run logs.

**Step 6: Verify final config**

```bash
curl -s "https://visa.homiapp.xyz/api/bots/7" | jq '{
  id, status, locale, proxyProvider, targetDateBefore,
  maxReschedules, rescheduleCount, pollEnvironments,
  currentConsularDate, speculativeTimeFallback
}'
```

Expected: proxyProvider=direct, targetDateBefore=2026-07-01, rescheduleCount=0, maxReschedules=1, status=active, speculativeTimeFallback=false.
  </action>
  <verify>
    <automated>curl -s "https://visa.homiapp.xyz/api/bots/7" | jq -e '.proxyProvider == "direct" and .targetDateBefore == "2026-07-01" and .speculativeTimeFallback == false'</automated>
  </verify>
  <acceptance_criteria>
    - curl GET /api/bots/7 returns proxyProvider="direct"
    - curl GET /api/bots/7 returns targetDateBefore="2026-07-01"
    - curl GET /api/bots/7 returns status="active"
    - curl GET /api/bots/7 returns rescheduleCount=0
    - curl GET /api/bots/7 returns speculativeTimeFallback=false
  </acceptance_criteria>
  <done>Bot 7 running with direct provider and targetDateBefore=2026-07-01; speculativeTimeFallback=false (disabled); code deployed to RPi; DB schema pushed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Bot 7 poll chain is healthy with new config</name>
  <files><!-- deploy+config only, no source files modified --></files>
  <action>
Human verification of Bot 7 system health after deploying code changes and switching config. No code changes -- this is a manual observation checkpoint.

What was built:
- Diagnostic logging for phantom dates (raw times response + timing instrumentation)
- Peru verification pattern refactoring (SUCCESS_PATTERNS array + extended diagnostic logging)
- Speculative time fallback (tries 10:15, 10:00, 07:30 when times are empty -- but DISABLED via speculativeTimeFallback=false)
- Bot 7 switched to direct provider with targetDateBefore=2026-07-01

Verification steps:
1. Wait 2-3 minutes for at least 1 poll cycle to complete
2. Check recent poll logs: `curl -s "https://visa.homiapp.xyz/api/bots/7/logs/polls?limit=5" | jq '.[].status'`
   Expected: recent polls show `filtered_out` (normal -- most Lima dates are 2027+)
3. Check RPi logs for diagnostic output: `source .env && sshpass -p "$RPI_PASS" ssh rpi "journalctl -u visa-trigger --since '3 min ago' --no-pager | tail -20"`
4. Verify no crash errors: `source .env && sshpass -p "$RPI_PASS" ssh rpi "journalctl -u visa-trigger --since '3 min ago' --no-pager | grep -i 'error' | tail -5"`
  </action>
  <verify>
    <automated>curl -s "https://visa.homiapp.xyz/api/bots/7/logs/polls?limit=3" | jq -e 'length > 0'</automated>
  </verify>
  <acceptance_criteria>
    - Bot 7 has recent poll logs (within last 5 minutes)
    - No crash or unhandled error in RPi logs after deployment
    - Poll status is filtered_out or ok (not session_expired or error)
  </acceptance_criteria>
  <done>User confirms Bot 7 poll chain is healthy with new config; system is armed and monitoring for next bookable date; speculativeTimeFallback remains false until user reviews dry-run logs and enables via Plan 02 checkpoint</done>
</task>

</tasks>

<verification>
- Bot 7 API shows: proxyProvider=direct, targetDateBefore=2026-07-01, status=active, rescheduleCount=0, speculativeTimeFallback=false
- Recent poll logs show the bot is actively polling (filtered_out status expected for most polls)
- No crash or session errors in RPi logs after deployment
</verification>

<success_criteria>
1. Bot 7 uses direct provider (confirmed via API)
2. Bot 7 targetDateBefore is 2026-07-01 (confirmed via API)
3. Bot 7 speculativeTimeFallback is false (disabled until user enables)
4. Bot 7 poll chain is active and healthy (confirmed via poll logs)
5. Diagnostic logging is live (visible in RPi logs when bookable dates appear)
6. Speculative fallback code is deployed but inactive (flag=false)
7. User has verified system health
</success_criteria>

<output>
After completion, create `.planning/phases/04-bot-7-peru-research-y-plan-para-lograr-reagendamiento-exitoso/04-03-SUMMARY.md`
</output>
