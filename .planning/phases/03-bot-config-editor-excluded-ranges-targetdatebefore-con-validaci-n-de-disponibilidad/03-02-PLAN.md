---
phase: 03-bot-config-editor-excluded-ranges-targetdatebefore-con-validaci-n-de-disponibilidad
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/api/dashboard.ts
autonomous: true
requirements:
  - CFG-TARGET-01
  - CFG-TARGET-02
  - CFG-TARGET-03
  - CFG-VALID-01

must_haves:
  truths:
    - "Modal shows current targetDateBefore value or 'sin limite' when null"
    - "User can type a new date in the input and click guardar to save"
    - "Guardar triggers validation: fetches available-dates, filters, blocks if 0 remain"
    - "Limpiar button clears targetDateBefore to null with confirm dialog"
    - "After save, lastBot is updated in memory and section re-renders without page reload"
  artifacts:
    - path: "src/api/dashboard.ts"
      provides: "renderCfgTargetSection function + validation flow"
      contains: "renderCfgTargetSection"
  key_links:
    - from: "src/api/dashboard.ts renderCfgTargetSection"
      to: "/api/bots/:id/available-dates"
      via: "fetchJ in cfgValidateAndSave"
      pattern: "available-dates"
    - from: "src/api/dashboard.ts cfgSaveTarget"
      to: "PUT /api/bots/:id"
      via: "fetch PUT with targetDateBefore"
      pattern: "targetDateBefore"
---

<objective>
Implement the targetDateBefore editing section inside the config modal: date input, guardar/limpiar buttons, validation flow that checks available dates before saving.

Purpose: Operators can change the target date cutoff without touching the DB, with safety validation ensuring the bot still has dates to work with.
Output: Fully functional Section A of the config modal.
</objective>

<execution_context>
@/Users/juanortega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanortega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-bot-config-editor-excluded-ranges-targetdatebefore-con-validaci-n-de-disponibilidad/03-CONTEXT.md
@.planning/phases/03-bot-config-editor-excluded-ranges-targetdatebefore-con-validaci-n-de-disponibilidad/03-UI-SPEC.md
@.planning/phases/03-bot-config-editor-excluded-ranges-targetdatebefore-con-validaci-n-de-disponibilidad/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — modal scaffold creates these DOM elements: -->
<div id="cfgSectionA"> contains <div id="cfgTargetContent"></div>
<!-- Plan 01 variables available in JS scope: -->
var cfgSavedTarget = lastBot.targetDateBefore || null;
var cfgSavedRanges = deep copy of lastBot.excludedDateRanges;
var cfgPendingRanges = deep copy of cfgSavedRanges;
var lastBot — full bot object from GET /api/bots/:id
var BID — current bot ID

<!-- Plan 01 endpoint: -->
GET /api/bots/:id/available-dates -> { dates: string[], pollAge: number, stale: boolean }

<!-- Existing helpers: -->
function fetchJ(u) — fetch wrapper
function fmtD(s) — "YYYY-MM-DD" -> "DD mmm YYYY"
function showToast(msg, ok)
var API — base API URL (e.g. '' or '/api' prefix — check dashboard.ts)

<!-- PUT /api/bots/:id accepts: { targetDateBefore: string | null } -->
<!-- Response: { success: true } -->

<!-- CSS classes from Plan 01 already available: -->
.cfg-date-input, .cfg-date-null, .cfg-btn-row, .cfg-btn, .cfg-btn-save, .cfg-btn-clear, .cfg-btn-loading, .cfg-err

<!-- Validation logic from 03-CONTEXT.md: -->
1. date < newTargetDateBefore (if null, skip filter)
2. date < currentConsularDate (always)
3. date NOT IN excluded_ranges (all ranges)
If result === 0 -> show error, block save
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement renderCfgTargetSection + validation + save/clear</name>
  <files>src/api/dashboard.ts</files>
  <read_first>
    - src/api/dashboard.ts — search for "openCfgModal" to find Plan 01 scaffold code and understand where to add renderCfgTargetSection
    - src/api/dashboard.ts — search for "cfgSavedTarget" to see the variable declarations
    - src/api/dashboard.ts — search for "fetchJ" to confirm the fetch helper signature
    - src/api/dashboard.ts — search for "showToast" to confirm the toast helper signature
    - src/api/dashboard.ts — search for "API" variable definition to confirm API base path
    - .planning/phases/03-bot-config-editor-excluded-ranges-targetdatebefore-con-validaci-n-de-disponibilidad/03-UI-SPEC.md lines 241-358 (Section A interaction + validation flow)
    - .planning/phases/03-bot-config-editor-excluded-ranges-targetdatebefore-con-validaci-n-de-disponibilidad/03-CONTEXT.md lines 61-86 (UX flow for targetDateBefore + validation)
  </read_first>
  <action>
Add the following functions to `src/api/dashboard.ts`, near the other config modal functions added by Plan 01 (after `cfgEscHandler`).

**1. `renderCfgTargetSection()` function:**

Populates `#cfgTargetContent` with:
- If `cfgSavedTarget` is set: `<input type="date" id="cfgDateInput" class="cfg-date-input" value="{cfgSavedTarget}">` 
- If `cfgSavedTarget` is null: `<div class="cfg-date-null">sin limite</div>` AND a hidden `<input type="date" id="cfgDateInput" class="cfg-date-input" style="display:none" value="">` (shown when user wants to set a value — add a small "establecer" link that shows the input and hides the null text)
- Button row: `<div class="cfg-btn-row"><button class="cfg-btn cfg-btn-save" id="cfgTargetSave" onclick="cfgSaveTarget()">guardar</button><button class="cfg-btn cfg-btn-clear" id="cfgTargetClear" onclick="cfgClearTarget()" style="{display:none if null}">limpiar</button></div>`
- Error div: `<div class="cfg-err" id="cfgTargetErr" style="display:none"></div>`

When value is null, show a clickable `<span style="font-size:9px;color:var(--accent);cursor:pointer;text-decoration:underline" onclick="...">establecer fecha limite</span>` that hides the `.cfg-date-null` div, shows the input with `display:block`, and shows the clear button.

**2. `cfgSaveTarget()` function:**

Flow (matching 03-UI-SPEC.md validation flow lines 338-358):
1. Read `document.getElementById('cfgDateInput').value`. If empty and cfgSavedTarget was also null, show toast "sin cambios" and return.
2. Set save button to loading: add class `cfg-btn-loading`, set `disabled=true`, change text to `validando...`.
3. `fetchJ(API+'/bots/'+BID+'/available-dates')` — GET available dates.
4. On fetch success: filter dates using `cfgFilterDates(dates, newTarget, lastBot.currentConsularDate, cfgPendingRanges)`.
5. If 0 dates remain: show `#cfgTargetErr` with text `Sin esta configuracion el bot no podria reagendar a ninguna fecha disponible. Ajusta los rangos o la fecha limite.` Revert button to idle.
6. If 1+ dates: hide error, call `PUT /api/bots/${BID}` with `{ targetDateBefore: newTarget }`.
7. On PUT success: `showToast('Fecha limite guardada', true)`, update `lastBot.targetDateBefore = newTarget`, update `cfgSavedTarget = newTarget`, re-render section.
8. On PUT failure: `showToast('Error al guardar. Reintenta.', false)`, revert button to idle.
9. On fetch failure: show `#cfgTargetErr` with `Error al verificar disponibilidad. Reintenta.` Revert button to idle.

**3. `cfgClearTarget()` function:**

1. Show `confirm('Quitar la fecha limite? El bot buscara cualquier fecha anterior a la cita actual.')`.
2. If confirmed: `PUT /api/bots/${BID}` with `{ targetDateBefore: null }`. No validation needed (removing limit only increases dates).
3. On success: `showToast('Fecha limite eliminada', true)`, update `lastBot.targetDateBefore = null`, update `cfgSavedTarget = null`, re-render section.
4. On failure: `showToast('Error al guardar. Reintenta.', false)`.

**4. `cfgFilterDates(dates, targetDateBefore, currentConsularDate, excludedRanges)` function:**

Shared utility (also used by Plan 03). Returns filtered array:
```javascript
function cfgFilterDates(dates, target, consular, ranges){
  return dates.filter(function(d){
    if(target && d >= target) return false;
    if(consular && d >= consular) return false;
    for(var i=0;i<ranges.length;i++){
      if(d >= ranges[i].startDate && d <= ranges[i].endDate) return false;
    }
    return true;
  });
}
```

**5. Wire renderCfgTargetSection into openCfgModal:**

In the `openCfgModal` function (from Plan 01), replace the comment `// Plan 02 will call renderCfgTargetSection()` with the actual call: `renderCfgTargetSection();`

**PUT fetch pattern** (use raw fetch, not fetchJ which is GET-only):
```javascript
fetch(API+'/bots/'+BID,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({targetDateBefore:val})})
.then(function(r){if(!r.ok)throw new Error(r.status);return r.json()})
```
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "renderCfgTargetSection" src/api/dashboard.ts returns at least 2 (definition + call in openCfgModal)
    - grep -c "cfgSaveTarget" src/api/dashboard.ts returns at least 2 (function def + onclick)
    - grep -c "cfgClearTarget" src/api/dashboard.ts returns at least 2 (function def + onclick)
    - grep -c "cfgFilterDates" src/api/dashboard.ts returns at least 1 (function definition)
    - grep "Fecha limite guardada" src/api/dashboard.ts returns 1 match (toast message)
    - grep "Fecha limite eliminada" src/api/dashboard.ts returns 1 match (toast message)
    - grep "Sin esta configuracion" src/api/dashboard.ts returns at least 1 match (validation error)
    - grep "available-dates" src/api/dashboard.ts returns at least 1 match (fetch call)
    - grep "cfgDateInput" src/api/dashboard.ts returns at least 2 matches (HTML + JS read)
    - npm test passes with 0 failures
  </acceptance_criteria>
  <done>targetDateBefore section fully functional: shows current value, saves with validation, clears with confirmation, error messages display correctly</done>
</task>

</tasks>

<verification>
1. `npm test` passes
2. Open modal on bot with targetDateBefore set -> shows date, guardar/limpiar buttons
3. Open modal on bot with targetDateBefore null -> shows "sin limite", link to set
4. Change date, click guardar -> validates against available-dates -> saves or shows error
5. Click limpiar -> confirm dialog -> clears to null
</verification>

<success_criteria>
- targetDateBefore editable via date input in modal
- Guardar validates available dates before saving
- Limpiar clears with confirm dialog
- Validation error shown when 0 dates remain
- Toast messages for success/failure
- lastBot updated in memory after save (no page reload needed)
- npm test green
</success_criteria>

<output>
After completion, create `.planning/phases/03-bot-config-editor-excluded-ranges-targetdatebefore-con-validaci-n-de-disponibilidad/03-02-SUMMARY.md`
</output>
