---
phase: 05-open-source-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/db/schema.ts
  - src/api/logs.ts
  - src/trigger/ensure-chain.ts
  - src/trigger/poll-cron.ts
  - src/api/bots-me.test.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "isScout and isSubscriber columns removed from schema (CLAUDE.md says they are legacy)"
    - "dispatch_logs references removed from query code (table definition kept for data preservation)"
    - "One-off personal analysis scripts removed from git tracking"
    - "All tests pass after cleanup"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Schema without legacy scout/subscriber columns"
    - path: ".gitignore"
      provides: "scripts/ one-off files excluded"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/api/logs.ts"
      via: "dispatch_logs query removal"
      pattern: "dispatch_logs"
---

<objective>
Code simplification: remove legacy dead code (scout/subscriber architecture, dispatch_logs queries) and clean up one-off scripts from git tracking.

Purpose: The codebase has accumulated legacy code from the removed scout/subscriber architecture (isScout, isSubscriber columns still in schema, dispatch_logs query code in logs.ts) and ~90 one-off analysis/debugging scripts tracked in git. For a public release, remove the dead code and untrack personal operational scripts while keeping the core scripts referenced in package.json.

Output: Cleaner schema, removed dead query code, scripts cleaned up
</objective>

<execution_context>
@/Users/juanortega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanortega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@src/db/schema.ts
@src/api/logs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy scout/subscriber code and dispatch_logs queries</name>
  <files>src/db/schema.ts, src/api/logs.ts, src/trigger/ensure-chain.ts, src/trigger/poll-cron.ts, src/api/bots-me.test.ts</files>
  <read_first>src/db/schema.ts, src/api/logs.ts, src/trigger/ensure-chain.ts, src/trigger/poll-cron.ts, src/api/bots-me.test.ts</read_first>
  <action>
1. **Remove isScout and isSubscriber from bots schema** (`src/db/schema.ts`):
   - Delete the `isScout: boolean('is_scout').notNull().default(false)` line
   - Delete the `isSubscriber: boolean('is_subscriber').notNull().default(true)` line
   - KEEP the `dispatch_logs` table definition (CLAUDE.md: "Existe para no perder datos de runs pasados")
   - Do NOT run `db:push` — this is a schema-only cleanup. The columns can be dropped in a separate migration if desired.

2. **Remove dispatch_logs query code** (`src/api/logs.ts`):
   - Find the endpoint/handler that queries `dispatch_logs` (around the comments at lines mentioning "Get subscriber's facility to filter dispatch_logs")
   - Remove the query code that reads from dispatch_logs. Keep the table definition in schema.ts.
   - If there's an API route serving dispatch_logs data, remove the route handler.

3. **Remove isScout/isSubscriber references** in other files:
   - `src/trigger/ensure-chain.ts` — remove any `isScout`/`isSubscriber` filters or conditions
   - `src/trigger/poll-cron.ts` — remove any `isScout`/`isSubscriber` filters or conditions
   - `src/api/bots-me.test.ts` — remove `isScout`/`isSubscriber` from test fixtures/assertions

4. **Run `npm test`** to verify no regressions. Fix any import errors or type errors caused by removed fields.

IMPORTANT: Only modify files where `isScout`, `isSubscriber`, or `dispatch_logs` query code exists. Do NOT touch `dispatch_logs` table definition in schema.ts.
  </action>
  <verify>
    <automated>cd /Users/juanortega/visa-scraper && grep -rn 'isScout\|isSubscriber\|is_scout\|is_subscriber' src/ | grep -v 'dispatch_logs' | grep -v node_modules | wc -l | xargs -I {} sh -c 'if [ {} -eq 0 ]; then echo "PASS: no scout/subscriber refs"; else echo "FAIL: {} refs remain"; fi' && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'isScout\|isSubscriber\|is_scout\|is_subscriber' src/ | wc -l` returns 0
    - `grep -c 'dispatch_logs' src/api/logs.ts` returns 0 (query code removed)
    - `grep -c 'dispatch_logs' src/db/schema.ts` returns >= 1 (table definition preserved)
    - `npm test` passes with 0 failures
  </acceptance_criteria>
  <done>Legacy scout/subscriber columns removed from schema. dispatch_logs query code removed from logs.ts. All isScout/isSubscriber references purged from src/. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Untrack one-off personal scripts from git</name>
  <files>.gitignore</files>
  <read_first>.gitignore</read_first>
  <action>
1. The core scripts (referenced in package.json, should STAY tracked) are:
   - `scripts/login.ts`
   - `scripts/test-fetch.ts`
   - `scripts/session-duration-test.ts`
   - `scripts/session-idle-test.ts`
   - `scripts/test-reschedule-e2e.ts`
   - `scripts/monitor.ts`
   - `scripts/test-notifications.ts`
   - `scripts/deploy-rpi.sh`

   Also keep general-purpose utility scripts that any operator would need:
   - `scripts/activate-bot.ts`
   - `scripts/set-bot-active.ts` (if it exists and is tracked)
   - `scripts/set-bot-status.ts` (if it exists and is tracked)
   - `scripts/add-excluded-dates.ts`

2. All OTHER scripts in `scripts/` are one-off personal analysis/debugging scripts (check-bot19-detail.ts, analyze-bot7-tcp.ts, bot7-sniper.ts, get-creds-19.ts, reschedule-bot30-may.ts, etc.). These contain hardcoded bot IDs, personal names, and operational details.

3. Strategy: Instead of gitignoring all of scripts/ (which would hide the core scripts), untrack the personal scripts individually using `git rm --cached`:

```bash
# Get list of tracked scripts
git ls-files -- 'scripts/*.ts' 'scripts/*.sh'

# Remove all tracked scripts EXCEPT the core ones
# (the executor should build this list dynamically by comparing tracked files against the keep list above)
```

4. Add a comment in .gitignore explaining the scripts/ policy:

```
# One-off analysis scripts (keep only core scripts tracked)
# Core scripts: login.ts, test-fetch.ts, test-reschedule-e2e.ts, monitor.ts,
#   test-notifications.ts, session-duration-test.ts, session-idle-test.ts,
#   activate-bot.ts, add-excluded-dates.ts, deploy-rpi.sh
# All other scripts/*.ts are personal/operational — add to .gitignore if new ones appear
```

5. Do NOT delete the local files — only untrack from git with `git rm --cached`.

6. Verify `npm run login -- --help 2>/dev/null || true` still works (script file still exists locally).
  </action>
  <verify>
    <automated>cd /Users/juanortega/visa-scraper && TRACKED=$(git ls-files -- 'scripts/*.ts' | wc -l) && echo "Tracked scripts: $TRACKED" && test "$TRACKED" -le 15 && echo "PASS: <= 15 scripts tracked" || echo "FAIL: $TRACKED scripts still tracked (expected <= 15)"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -- scripts/login.ts` returns `scripts/login.ts` (core script still tracked)
    - `git ls-files -- scripts/test-reschedule-e2e.ts` returns the file (core script still tracked)
    - `git ls-files -- scripts/monitor.ts` returns the file (core script still tracked)
    - `git ls-files -- 'scripts/*.ts' | wc -l` returns <= 15
    - `git ls-files -- scripts/bot7-sniper.ts` returns empty (personal script untracked)
    - `git ls-files -- scripts/get-creds.ts` returns empty (credential script untracked)
    - `git ls-files -- scripts/check-bot12.ts` returns empty (personal script untracked)
    - `test -f scripts/bot7-sniper.ts && echo "local file preserved"` shows file still exists locally
  </acceptance_criteria>
  <done>One-off personal/debugging scripts untracked from git. Core scripts (login, test-fetch, test-reschedule, monitor, etc.) remain tracked. All local files preserved.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'isScout\|isSubscriber\|is_scout\|is_subscriber' src/` returns 0 matches
2. `npm test` passes
3. `git ls-files -- 'scripts/*.ts' | wc -l` returns <= 15
4. Core scripts still tracked and functional
5. dispatch_logs table definition preserved in schema.ts
</verification>

<success_criteria>
- Legacy scout/subscriber code fully removed from src/
- dispatch_logs query code removed (table definition preserved)
- Only core utility scripts tracked in git; personal analysis scripts untracked
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-open-source-cleanup-readme-secrets-audit-simplify-logic-gitignore-hardening/05-03-SUMMARY.md`
</output>
